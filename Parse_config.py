
class ParseConfig:
    def __init__(self, setup, config):
        self.setup = setup
        self.config = str(config)
        self.get_region(self.setup)
        
    def get_region(self, setup):
        pass
        #print(setup["Region"])
        
    
    
        
        
        
        